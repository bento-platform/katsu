dist: bionic
language: python
python:
  - "3.7"
services:
  - postgresql
addons:
  postgresql: "11"
  apt:
    packages:
      - postgresql-11
      - postgresql-contrib-11
install:
  - pip install -r requirements.txt
script:
  POSTGRES_USER="postgres" POSTGRES_PASSWORD="" python3 ./manage.py test
